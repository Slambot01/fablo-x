# Generated by Fablo (poc/fabric-x-integration)
# P2P websocket mesh routing for endorser2
#
# Identical route entries to endorser1 — every node in the mesh
# needs to know about every other node regardless of which node
# this config belongs to.

routes:
  endorser1:
    - endorser1.example.com:9301
  endorser2:
    - endorser2.example.com:9401
