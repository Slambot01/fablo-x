# Generated by Fablo (poc/fabric-x-integration)
# P2P websocket mesh routing for endorser1
#
# Every FSC node gets its own routing-config.yaml listing ALL
# other nodes in the mesh by logical name → host:p2p_port
#
# Schema source: fabric-x/samples/tokens/conf/endorser1/routing-config.yaml
# Note: auditor route included — present in real source even without
# an active auditor service in this 2-endorser POC network.

routes:
  endorser1:
    - endorser1.example.com:9301
  endorser2:
    - endorser2.example.com:9401
