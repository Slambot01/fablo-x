# Source: fabric-x/samples/tokens/conf/<%= node.name %>/routing-config.yaml
routes:
<% allFscNodes.filter(function(n) { return !n.excludeFromResolvers; }).forEach(function(fscNode) { -%>
  <%= fscNode.name %>:
    - <%= fscNode.name %>.example.com:<%= fscNode.ports.p2p %>
<% }); -%>
