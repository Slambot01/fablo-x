<%# Routing config template — Fabric-X mode %>
<%# Source reference: fabric-x/samples/tokens/conf/endorser1/routing-config.yaml %>

routes:
<% allFscNodes.forEach(function(node) { -%>
  <%= node.name %>:
    - <%= node.name %>.example.com:<%= node.ports.p2p %>
<% }); -%>
